- name: check a number is less than 10 or not
  hosts: local
  connection: local
  Vars:
     Number_of_sessions: 7
  tasks:
  - name: is ansible sessions more than 5
    ansible.builtin.debug:
      msg: "yes,more than 5 sessions"
    when: Number_of_sessions > 5